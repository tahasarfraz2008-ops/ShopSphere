# If `mysqlclient` fails to install on your machine (common on Windows),
# install `pymysql` instead (see requirements.txt) and uncomment the two
# lines below. They make PyMySQL pretend to be the MySQLdb driver Django
# expects, so no other code needs to change.
#
# import pymysql
# pymysql.install_as_MySQLdb()
